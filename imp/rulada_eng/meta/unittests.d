/** Unit tests for the meta functions
 * They are in a seperate file so that they are not executed when
 * unit tests are enabled in user code.
 */
module meta.unittests;

// Just import all the modules.
import meta.math;
import meta.conv;
import meta.ctype;
import meta.nameof;
