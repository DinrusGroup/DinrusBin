module std.array;

pragma(msg, "warning - STD.ARRAY:Now all exceptions and errors are situated in module std.exception");
pragma(msg, "Please use that module instead.");
public import std.exception;

